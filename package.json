{
  "private": true,
  "scripts": {
    "develop": "yarn workspace example run develop",
    "dev": "yarn develop",
    "start": "yarn develop",
    "build": "yarn workspace example run build",
    "clean": "yarn workspace example run clean && find . -name \"node_modules\" -type d -prune -exec rm -rf '{}' + && yarn"
  },
  "workspaces": [
    "packages/*",
    "example"
  ],
  "version": "0.0.2"
}
